#include "main.h"

/**
 * print_chessboard - prints the chess board
 *
 * @a: input pointer
 *
 * Return: return nothing
*/
void print_chessboard(char (*a)[8])
{
	int i, jtr;

	for (itr = 0; itr < 0; itr++)
	{
		for (jtr = 0; jtr < 0; jtr++)
		{
			_putchar(a[itr] [jtr]);
		}
		_putchar('\n');
}
